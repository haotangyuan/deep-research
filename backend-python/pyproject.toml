[project]
name = "deep-research-python-backend"
version = "0.1.0"
description = "FastAPI backend for Deep Research"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
